type EvmEvent @entity {
    id: ID!
    block: Int!
    name: String! @index
    params: JSON!
}

type EvmFunction @entity {
    id: ID!
    block: Int!
    txHash: String!
    name: String! @index
    params: JSON!
}
