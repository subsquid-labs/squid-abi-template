manifestVersion: subsquid.io/v0.1
name: abi-template
version: 1
description:
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]